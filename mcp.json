{
  "mcpServers": {
    "thor": {
      "command": "${powerDir}/server/start.sh",
      "timeout": 600000,
      "transport": "stdio",
      "args": [],
      "cwd": "${powerDir}",
      "env": {
        "HOME": "${env:HOME}",
        "AWS_DEFAULT_REGION": "us-east-1",
        "PATH": "/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin"
      }
    }
  }
}
