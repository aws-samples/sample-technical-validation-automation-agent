// Package validator drives the per-control Bedrock Converse calls,
// orchestrates parallelism + consensus, and returns structured Result
// values that downstream renderers consume without re-parsing.
package validator
